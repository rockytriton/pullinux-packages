tar -xf $filename
pckname=${filename%.t*}
cd phonon-4.11.1 || cd $pckname

mkdir build &&
cd    build &&

cmake -DCMAKE_INSTALL_PREFIX=/usr \
      -DCMAKE_BUILD_TYPE=Release  \
      .. &&
make

make DESTDIR=$pckdir install

