tar -xf $filename
pckname=${filename%.t*}
cd p7zip-17.03 || cd $pckname

sed '/^gzip/d' -i install.sh

make all3 && make DEST_DIR=$pckdir DEST_HOME=/usr \
     DEST_MAN=/usr/share/man \
     DEST_SHARE_DOC=/usr/share/doc/p7zip-17.03 install

