tar -xf $filename
pckname=${filename%.t*}
cd tiff-4.2.0 || cd $pckname

mkdir -p libtiff-build &&
cd       libtiff-build 

cmake -DCMAKE_INSTALL_DOCDIR=/usr/share/doc/libtiff-4.2.0 \
      -DCMAKE_INSTALL_PREFIX=/usr -G Ninja .. 

ninja && DESTDIR=$pckdir ninja install

