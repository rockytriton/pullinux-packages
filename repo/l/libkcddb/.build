tar -xf $filename
pckname=${filename%.t*}
cd libkcddb-20.12.2 || cd $pckname

mkdir build && cd build
cmake -DCMAKE_INSTALL_PREFIX=$KF5_PREFIX -DCMAKE_PREFIX_PATH=$QT5DIR \
      -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF -Wno-dev ..
make && make DESTDIR=$pckdir install

