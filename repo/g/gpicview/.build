tar -xf $filename
pckname=${filename%.t*}
cd gpicview-0.2.5 || cd $pckname

./configure --prefix=/usr 

make && make DESTDIR=$pckdir install

sed -i 's/Utility;//' $pckdir/usr/share/applications/gpicview.desktop

