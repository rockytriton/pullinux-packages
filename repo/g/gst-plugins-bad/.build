tar -xf $filename
pckname=${filename%.t*}
cd gst-plugins-bad-1.18.3 || cd $pckname

mkdir build && cd build

meson  --prefix=/usr       \
       -Dbuildtype=release \
       -Dpackage-origin=http://www.linuxfromscratch.org/blfs/view/svn/ \
       -Dpackage-name="GStreamer 1.18.3 Pullinux"

ninja && DESTDIR=$pckdir ninja install

