tar -xf $filename
pckname=${filename%.t*}
cd xorg-server-1.20.10 || cd $pckname

./configure $XORG_CONFIG --enable-glamor         \
            --enable-suid-wrapper   \
            --with-xkb-output=/var/lib/xkb

make && make DESTDIR=$pckdir install

mkdir -pv $pckdir/etc/X11/xorg.conf.d

